# Screenshots needed

One image, referenced from the main README, that needs a real desktop to
capture — no way to fake it convincingly in a container. Once captured,
drop it in this folder with the exact filename below and delete this
checklist file.

(The other screenshots the README uses — `installer-installing.png` and
`installer-connected.png` (the dashboard, fakeable in a container, so
generated automatically by CI — see `docker/capture-all-screenshots.sh`,
don't hand-edit, CI overwrites on every push) and
`resolve-external-scripting.png` (captured 2026-09-13) — don't belong here.)

## `desktop-file-trust.png`

Right-click on the extracted `davinci-aac-support.desktop` file in your file
manager, showing the "Allow Launching" (or equivalent trust/permissions)
option. Crop tightly to just the context menu — no need to show the rest of
the desktop.

---

Use whatever screenshot tool you're comfortable with (e.g. GNOME's
Shift+Ctrl+Print Screen for a selected region). Keep it tight to just the
relevant UI — nothing else on screen needs to be in frame.
